# Root STATUS/NOTES Policy

For this session, do not create or recreate root-level `STATUS.md` or `NOTES.md`.

Those prior instructions are erroneous for this repository workflow. Any session notes or status updates should go under `.agents/context/` instead.
