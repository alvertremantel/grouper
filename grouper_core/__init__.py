"""Grouper Core — shared data layer (database, models, config, colors).

This package has zero GUI dependencies. Safe for import from any context:
desktop app, sync server, CLI, or headless scripts.
"""

from grouper_core._version import __version__

__all__ = ["__version__"]
